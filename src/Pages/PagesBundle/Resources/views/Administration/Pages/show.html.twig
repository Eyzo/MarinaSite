{% extends '::layout/layout-administration.html.twig' %}

{% block body %}
    <h1>Pages</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ entity.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ entity.contenu }}</td>
            </tr>
        </tbody>
    </table>

        <a  style="display: block;margin-top: 10px;width: 150px;" class="button-success" href="{{ path('adminPages_edit', { 'id': entity.id }) }}">
            Editer
        </a>
        <a  style="margin-top: 10px;float: left;" class="button alt" href="{{ path('adminPages') }}">
            Retour à la liste des pages
        </a>


    {{ form(delete_form) }}
{% endblock %}
