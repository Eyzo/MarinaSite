{% extends '::layout/layout-administration.html.twig' %}

{% block body -%}
    <h1>Categories</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
     <li>
        <a class="button-success" href="{{ path('admin_categories_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>       
    <li>
        <a class="button alt" style="margin-top: 10px;float: left;" href="{{ path('admin_categories') }}">
            Retour a la liste des catégories
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
